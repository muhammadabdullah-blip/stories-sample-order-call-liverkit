# FastAPI backend
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0

# LiveKit (shared by both FastAPI and agent)
livekit>=0.18.2,<1.1
livekit-agents>=0.12.11,<1.0
livekit-plugins-openai>=0.10.17,<1.0
livekit-plugins-deepgram>=0.6.17,<1.0
livekit-plugins-silero>=0.7.4,<1.0
livekit-plugins-cartesia

# HTTP client (agent → FastAPI webhooks)
httpx>=0.27.0

# Supabase
supabase>=2.5.0

# Utilities
python-dotenv~=1.0
